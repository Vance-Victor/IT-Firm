@model IEnumerable<IT_Firm.Models.StaffSkill>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StaffSkillName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StaffSkillName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StaffSkillID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StaffSkillID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StaffSkillID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
